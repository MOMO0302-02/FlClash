# 怎么给应用做正式签名

> 做完这件事之后，以后每次给你的安装包都能**直接覆盖升级**，订阅和设置全部保留。
> 不做的话，每个包都会被安卓当成"另一个应用"装上，数据不共用。

## 先说三件要知道的事

1. **密码只有你知道。** 这一步必须你本人做——AI 不经手密码。
2. **钥匙丢了就再也无法升级覆盖了。** 换一把钥匙 = 换一个应用，只能卸载重装、数据清空。所以：
   - 密码找个地方记牢（密码管理器 / 纸质本子都行）；
   - `android/app/keystore.jks` 这个文件**备份一份到别的地方**。
3. **不会泄露。** `android/.gitignore` 已经挡住了 `*.jks` 和 `local.properties`，这两样都不会被提交到仓库。

---

## 第一步：生成钥匙

打开 **PowerShell**（开始菜单搜 "PowerShell" 即可，不需要管理员权限），把下面这一整行粘进去回车：

```
& "F:\AI\FlClash\.toolchain\jdk\bin\keytool.exe" -genkeypair -v -keystore "F:\AI\FlClash\android\app\keystore.jks" -keyalg RSA -keysize 2048 -validity 10000 -alias clashparty
```

> 这里写的是完整路径，所以**在任何一个 PowerShell 窗口里都能直接跑**，不用先进项目目录、也不用跑项目的环境脚本。

它会依次问你：

| 它问什么 | 你怎么答 |
|---|---|
| `输入密钥库口令` | **自己设一个密码**，至少 6 位。输入时屏幕上不显示任何字符，这是正常的 |
| `再次输入新口令` | 同一个密码 |
| 姓名、组织单位、组织、城市、省份、国家代码 | **随便填**，直接回车留空也行。这些只是证书里的描述信息，不影响使用 |
| `是否正确？` | 输入 `y` 回车 |
| `输入 <clashparty> 的密钥口令`（如果问到） | **直接回车**，表示和上面那个密码相同 |

跑完之后 `F:\AI\FlClash\android\app\keystore.jks` 就出现了。

---

## 第二步：把密码填进配置

用记事本打开这个文件：

```
F:\AI\FlClash\android\local.properties
```

在**文件末尾**加上三行（把 `你的密码` 换成第一步设的那个）：

```
storePassword=你的密码
keyAlias=clashparty
keyPassword=你的密码
```

注意：
- 等号两边**不要加空格**；
- 密码里如果有 `\` 反斜杠，要写成两个 `\\`（这类文件的转义规则）。想省事就别用反斜杠。

---

## 第三步：告诉我

跟我说一声"钥匙弄好了"，我重新打一个包。那个包的包名会从 `com.clashparty.app.unsigned` 变成正式的 `com.clashparty.app`。

**⚠️ 换成正式签名的那一次，仍然要卸载旧的再装**——因为包名变了，安卓还是当成两个应用。**从那一次之后**才能一直覆盖升级。所以如果你现在的那个已经配好了订阅，先在里面导出一份备份（应用内有"备份与恢复"）。

---

## 出问题时

| 现象 | 原因 |
|---|---|
| 提示 `keytool` 不是命令 | 路径打错了，确认 `F:\AI\FlClash\.toolchain\jdk\bin\keytool.exe` 这个文件存在 |
| 打包出来包名还是 `.unsigned` | 三行配置没生效——检查 `local.properties` 里有没有拼错，或者 `keystore.jks` 没生成在 `android\app\` 下 |
| 忘了密码 | 没有找回的办法。删掉 `keystore.jks` 重做一遍即可，代价是已经装过正式版的话要卸载重装 |
