# ClashMO 项目状态

更新时间：2026-09-07（Asia/Shanghai）

## 一句话结论

项目主体已经迁移到 `F:\AI\ClashMO`；遗留目录 `F:\AI\FlClash` 是旧构建留下的空目录骨架，相关有效路径已修正，空目录已移除。本次只清理了有证据能够重新生成的缓存，工具链、安装包、核心、备份、测试证据和全部源码均保留。

## 当前目标

- 继续完成项目由旧名称和旧包路径向 ClashMO 的迁移。
- 保护当前尚未提交的大量改动，不执行批量还原或版本历史操作。
- 保留离线构建环境、Smart 内核、核心二进制、历史安装包和验证资料。
- 后续在当前状态上继续功能验证、整理变更并决定版本提交方式。

## Git 工作区

- 当前分支：`rename-to-clash`
- 当前版本：`7c831855efedceb1a72bd0b4c18da026593d0853`
- 版本说明：`Update changelog`
- 该版本时间：2026-07-11 12:35:22 +0000
- 本文档生成前的工作区记录：224 个修改、45 个删除、11 个未跟踪入口，共 280 条。
- 删除记录主要对应旧的 `com/follow/clash` Android 包路径；未跟踪内容中包含新的 `com/clashmo` 路径、设计资料、Smart 内核、工具链、产物备份和测试资料。

本次没有执行 `git clean`、`git reset`、`git checkout`、提交、推送或历史改写。

## 已确认的最近工作和验证

- `temp\baseline3.log` 记录过早期测试失败。
- `temp\baseline4.log` 记录了后续完整重跑结果：约 1,136 条测试通过，退出码为 0。
- 早期失败后已重跑通过的测试包括：
  - `dashboard_targets_test.dart`
  - `input_test.dart`
  - `views_smoke_test.dart`
- 上述日志是当时的测试证据，不代表本文档生成后重新运行了一遍全部测试。

## 遗留目录核查结果

清理前，`F:\AI\FlClash` 包含 45 个目录、0 个文件、0 字节：

- 2026-09-05 11:43 创建了一组 `release` 构建目录。
- 2026-09-05 21:04 创建了一组 `debug` 构建目录。
- 路径包含 `flutter_assets`、`native_assets`、`jniLibs` 和处理器架构目录，能够确认是 Flutter/Android 构建留下的空骨架。

没有发现旧路径存在于 Windows 计划任务、用户或系统环境变量、系统启动项、常见快捷方式、VS Code 用户配置或 Android Studio/JetBrains 常见配置中。

清理前发现的有效引用及处理：

| 位置 | 原问题 | 处理结果 |
|---|---|---|
| `.toolchain\download.mjs` | 下载目录写死为旧项目路径 | 改为根据脚本自身位置使用 `.toolchain\dl` |
| `.toolchain\android-user\avd\test35.ini` | 模拟器登记指向旧目录 | 改为 `F:\AI\ClashMO` |
| `test35.avd\hardware-qemu.ini` | 模拟器运行文件指向旧工具链 | 改为 `F:\AI\ClashMO` |
| `test35.avd\snapshots\default_boot\hardware.ini` | 默认快照指向旧工具链 | 改为 `F:\AI\ClashMO` |
| `test35.avd\emu-launch-params.txt` | 上次启动参数记录旧目录 | 改为 `F:\AI\ClashMO` |
| `.smart-merge\README.md` | 示例命令写死旧项目目录 | 改为使用命令执行时的当前目录 |
| `.toolchain\env.ps1` | 注释包含旧绝对路径 | 改为不依赖具体旧目录的迁移说明 |

修正后，相关配置中没有剩余的 `F:\AI\FlClash` 或 `F:/AI/FlClash` 引用。模拟器工具能够重新列出 `test35`，退出码为 0。

## 清理前的主要空间分层

| 内容 | 约占空间 | 用途和处理决定 |
|---|---:|---|
| `.toolchain` | 39.78 GiB | 离线 Flutter、Android、JDK、Gradle 和模拟器环境；保留 |
| `.git` | 0.56 GiB | Git 版本历史；保留，禁止删除 |
| `build` | 0.52 GiB | 可重新生成的根构建产物；已清理 |
| `dist` | 0.35 GiB | 5 个可安装 APK；保留 |
| `android` | 0.25 GiB | Android 源码与依赖内容混合；保留 |
| `.dart_tool` | 0.24 GiB | 可重新生成的 Dart/Flutter 项目缓存；已清理 |
| `libclash` | 0.18 GiB | 多种处理器架构的核心二进制；保留 |
| `_产物备份` | 0.12 GiB | 历史 APK 恢复材料；保留 |
| `plugins` | 0.09 GiB | 插件源码及测试缓存；只清理两个插件的可重建缓存 |
| `core` | 0.07 GiB | Go 内核源码及 Smart 合并目录；保留 |
| `temp` | 0.06 GiB | 测试日志、脚本、数据库资料和截图；保留 |
| `.design` | 3.31 MiB | 设计图和相关材料；保留 |
| `.smart-merge` | 35 KiB | Smart 内核补丁与合并说明；保留 |
| `.git-rewrite` | 0 字节 | 只有空目录；已清理 |

## 本次实际清理

已移除：

- `F:\AI\ClashMO\build`
- `F:\AI\ClashMO\.dart_tool`
- `F:\AI\ClashMO\plugins\proxy\build`
- `F:\AI\ClashMO\plugins\proxy\.dart_tool`
- `F:\AI\ClashMO\plugins\wifi_ssid\build`
- `F:\AI\ClashMO\plugins\wifi_ssid\.dart_tool`
- `F:\AI\ClashMO\.git-rewrite`（0 字节空目录）
- `F:\AI\FlClash`（0 文件、0 字节的空目录骨架）

按清理前文件大小合计，目标内容约为 905,173,948 字节。磁盘可用空间实测增加 904,867,840 字节，即约 862.9 MiB（0.843 GiB）。文件大小与磁盘实际占用略有差异属于正常现象。

## 恢复清单

本次没有归档不确定内容；所有不确定或不可替代内容均选择原地保留。

需要恢复已清理缓存时：

- 在项目根目录运行 `flutter pub get`，会重新生成根 `.dart_tool`。
- 运行相应的 Flutter 测试或构建，会重新生成根 `build`。
- 分别进入 `plugins\proxy` 和 `plugins\wifi_ssid` 执行 `flutter pub get`，会重新生成插件 `.dart_tool`。
- 运行对应插件测试，会重新生成插件 `build`。
- `.git-rewrite` 和 `F:\AI\FlClash` 删除前没有文件，不存在需要恢复的数据。

重新生成缓存会重新占用磁盘空间，并可能访问网络；本次没有执行这些恢复命令。

## 明确保留的大目录和原因

- `.toolchain`：项目的离线构建环境，删除后重建成本最高。
- `.git`：版本历史和当前工作区的基础。
- `dist`：包含 5 个安装包，其中有 Smart 调试版和多次 ClashParty 构建。
- `libclash`：应用运行和 Android 构建需要的核心二进制。
- `_产物备份`：包含历史调试安装包，是恢复材料。
- `core\Clash.Meta-smart`：带独立 Git 信息的完整 Smart 内核源码。
- `temp`：保存测试结果、数据库资料、脚本和截图，是当前工作的证据链。
- 所有未提交及未跟踪源码：属于正在进行的包名迁移和功能修改。

## 本次失败或异常

- 第一条 PowerShell 批量删除命令被本机安全策略拒绝，没有产生删除。
- 根 `build` 文件很多，首次删除超过单次命令等待时间；两个同目标删除进程曾同时继续运行，因此出现“文件已不存在”的提示。等待两个进程结束后，根 `build` 已完整移除。
- 一条早期只读盘点命令存在排序语法错误，随后使用简化命令成功重跑，没有产生文件改动。
- 除上述过程提示外，没有发现目标范围外的文件被删除。

## 验收结果

- 所有批准清理的目标路径均已确认不存在。
- `F:\AI\FlClash` 已确认不存在。
- 受保护的 `.toolchain`、`.git`、`dist`、`libclash`、`_产物备份`、`core\Clash.Meta-smart` 和 `temp` 均确认仍存在。
- `download.mjs` 通过 Node.js 语法检查，退出码为 0；没有实际运行下载。
- Android 模拟器工具能够列出 `test35`，退出码为 0；没有启动模拟器。
- 旧绝对路径在已知有效配置中已清零。

## 下一步

1. 如要继续开发，先运行 `flutter pub get` 恢复依赖索引。
2. 在准备验收当前大量源码改动时，运行 `flutter analyze --no-fatal-infos` 和 `flutter test --reporter expanded`。
3. 完整测试会重新生成部分已清理缓存，因此应在确实要继续开发或交付前运行。
4. 在任何提交前，先人工复核 224 个修改、45 个删除以及新的 `com/clashmo` 源码是否组成完整迁移。
5. 不要使用 `git clean` 清理未跟踪目录；其中包含工具链、设计资料、Smart 内核、备份和测试证据。
