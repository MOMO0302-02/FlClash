<?xml version="1.0" encoding="utf-8"?>
<!--
  应用标记的白色版：只给启动图标用（配蓝色底）。

  几何和 `ic_mark` 完全相同，只有颜色不同。**两份都要留着**：闪屏浅色模式的
  底是 #FFFFFF，白图形在上面等于隐形，那里必须用蓝色那一份。

  **为什么是猫**：Clash 这一系的客户端普遍用猫作为视觉符号，用户要求「图标需要
  clash 相关」。**但这只猫是从零画的几何图形**，不是描谁的图——原来那张
  `ic_cat.png` 和桌面版 Clash Party 的 `build/icon.png` 逐字节相同
  （MD5 都是 95f9dee7…），那是别人项目的美术资源，顶着它公开发布会让人以为是官方
  出品。桌面版那只是**整只坐着的猫**，这里是**正面猫头**，轮廓完全不同。

  ## 画成什么样，是被三个硬约束框出来的

  * **必须是纯矢量**。仓库里没有图像库，生成不了多尺寸位图；矢量在任何密度下都
    清晰，也不用维护 5 套 mipmap。
  * **必须在安全区内**。自适应图标画布是 108，系统会裁成圆形/方形/水滴等形状，
    内容要留在中心 72 的安全区里。这里图形占 x 30~78、y 22~86，四边都留够了。
  * **必须是能读的剪影**。这张图同时当通知栏和快捷开关的小图标用，而系统对那种
    图标**只取 alpha 通道整体染色**——颜色一律无效，只有轮廓有意义。所以闪电是用
    `evenOdd` 从猫头里**挖掉**的，不是叠在上面的另一种颜色；叠色在通知栏会糊成
    一整块实心，什么都看不出来。

  猫头和闪电写在同一个 path 的两段子路径里，靠 `evenOdd` 挖洞——分成两个 path
  就挖不出洞了。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFF"
        android:fillType="evenOdd"
        android:pathData="M30,22 L40,42 L68,42 L78,22 L78,58 C78,74 68,84 54,86 C40,84 30,74 30,58 Z M58,48 L46,66 L54,66 L50,78 L64,60 L56,60 Z" />
</vector>
